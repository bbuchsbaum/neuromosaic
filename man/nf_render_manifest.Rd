% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/montage_neurotabs.R
\name{nf_render_manifest}
\alias{nf_render_manifest}
\title{Build a Montage Render Manifest from an NFTab Dataset}
\usage{
nf_render_manifest(
  ds,
  data_feature,
  path_col = NULL,
  root = NULL,
  materialize_dir = NULL,
  map_id_cols = NULL,
  label_cols = map_id_cols,
  include_design = TRUE,
  stat_kind = "z",
  units = stat_kind,
  signed = TRUE,
  threshold = NULL,
  p = NULL,
  tail = "two_sided",
  connectivity = "18-connect",
  min_cluster_size = 10L,
  level = "subject",
  validate = TRUE,
  check_files = TRUE
)
}
\arguments{
\item{ds}{An `nftab` object.}

\item{data_feature}{Feature name containing the map to render.}

\item{path_col}{Optional design/observation column containing map paths. If
`NULL`, the adapter tries to infer it from the feature's ref encoding.}

\item{root}{Optional root used to resolve relative paths. Defaults to the
NFTab root when available.}

\item{materialize_dir}{Optional directory for backend-backed feature maps.
Required only when paths cannot be read directly; defaults to a temporary
directory.}

\item{map_id_cols, label_cols}{Observation columns used to build `map_id` and
`label`. Defaults to NFTab observation axes when available.}

\item{include_design}{Logical; include observation/design columns in the
render manifest?}

\item{stat_kind, units, signed, threshold, p, tail, connectivity, min_cluster_size}{Default render-manifest policy columns.}

\item{level}{Manifest `level` value. Defaults to `"subject"`.}

\item{validate}{Logical; run [validate_manifest()] before returning?}

\item{check_files}{Logical; require output paths to exist during validation?}
}
\value{
A render manifest data frame.
}
\description{
Converts a subject/observation-level `neurotabs` dataset into the
storage-agnostic render manifest consumed by [render_montage_report()].
File-backed feature rows are used directly; backend-backed rows are
materialized to NIfTI files in `materialize_dir`.
}

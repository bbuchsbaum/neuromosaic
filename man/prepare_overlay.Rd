% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/montage_overlay.R
\name{prepare_overlay}
\alias{prepare_overlay}
\title{Prepare Background and Statistic Volumes for Overlay Rendering}
\usage{
prepare_overlay(
  bg,
  stat,
  on_mismatch = c("error", "restamp"),
  tolerance = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{bg}{Background `NeuroVol` or file path.}

\item{stat}{Statistic `NeuroVol` or file path.}

\item{on_mismatch}{How to handle a grid mismatch. `"error"` fails loudly;
`"restamp"` copies the statistic array into the background space only when
dimensions match.}

\item{tolerance}{Numeric tolerance used when comparing space metadata.}
}
\value{
A list with `background`, `stat`, `reconciled`, and `action`.
}
\description{
Loads and reconciles a background/statistic volume pair before montage
rendering. By default, mismatched grids are a hard error. `on_mismatch =
"restamp"` is intentionally conservative: it only re-stamps the statistic
volume into the background space when voxel array dimensions already match.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/montage_labeller.R
\name{apply_montage_labeller}
\alias{apply_montage_labeller}
\title{Apply a Montage Manifest Labeller}
\usage{
apply_montage_labeller(
  manifest,
  labeller = NULL,
  entity_cols = NULL,
  check_files = FALSE
)
}
\arguments{
\item{manifest}{A render manifest data frame.}

\item{labeller}{`NULL`, a function, or a data frame keyed by `map_id`.}

\item{entity_cols}{Optional character vector of manifest columns passed to a
function labeller. Defaults to all columns in the row.}

\item{check_files}{Logical; passed to [validate_manifest()] after labelling.}
}
\value{
The labelled and validated manifest.
}
\description{
Applies the PRD labeller contract to a render manifest. A function labeller
is called once per row with an entity list and must return `title` (or
`label`), plus optional `short`, `description`, and `legend_semantics`.
A data-frame labeller is joined by `map_id` and may contain the same fields.
}

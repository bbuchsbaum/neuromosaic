% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/montage_stat_montage.R
\name{stat_montage}
\alias{stat_montage}
\title{Render a Statistic Volume Montage}
\usage{
stat_montage(
  bg,
  stat,
  threshold,
  tail = c("two_sided", "positive", "negative"),
  signed = TRUE,
  cap = NULL,
  limits = NULL,
  support_mask = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  initial_world_coord = NULL,
  zlevels = NULL,
  along = 3L,
  ncol = 3L,
  bg_cmap = "grays",
  ov_cmap = if (isTRUE(signed)) "blue-red" else "inferno",
  ov_alpha = 0.7,
  ov_alpha_mode = c("soft", "binary", "proportional", "ramp"),
  alpha_gamma = NULL,
  style = "report",
  on_mismatch = c("error", "restamp"),
  empty = c("error", "warning"),
  draw = TRUE
)
}
\arguments{
\item{bg}{Background `NeuroVol` or file path.}

\item{stat}{Statistic `NeuroVol` or file path.}

\item{threshold}{Positive numeric overlay threshold, or `NULL`/`NA` to show
finite values continuously without significance thresholding.}

\item{tail}{Tail mode: `"two_sided"`, `"positive"`, or `"negative"`.}

\item{signed}{Logical; use symmetric signed color limits and a diverging
palette?}

\item{cap}{Optional shared color cap. When supplied, overlay values are
clipped to this cap before plotting.}

\item{limits}{Optional finite increasing display limits. This is the
preferred scale contract for continuous maps; `cap` remains as a backward-
compatible shorthand for zero-centered or nonnegative scales.}

\item{support_mask}{Optional logical/numeric mask in statistic-map order.
Values outside the mask are hidden. This supports explicitly requested
primary-map masking without making it the auxiliary-map default.}

\item{title, subtitle, caption}{Text passed to `neuroim2::plot_overlay()`.}

\item{initial_world_coord}{Optional finite `(x, y, z)` world coordinate used
as the interactive view's initial crosshair. When omitted, the shared
display specification uses a tail-aware peak and then a field-of-view
centre fallback. It does not change the static slice selection.}

\item{zlevels, along, ncol}{Slice selection arguments passed through.}

\item{bg_cmap, ov_cmap}{Background and overlay color maps.}

\item{ov_alpha}{Overlay alpha.}

\item{ov_alpha_mode}{How overlay opacity tracks magnitude. `"binary"` gives
every suprathreshold voxel full `ov_alpha`; `"ramp"` ramps alpha linearly
from the threshold to the color cap; `"proportional"` sets alpha to
`|v| / cap`; `"soft"` uses a nonlinear self-tuning curve. Modes unknown to
the installed `neuroim2` fall back to its proportional alpha.}

\item{alpha_gamma}{Optional exponent for `ov_alpha_mode = "soft"`, forwarded
to `neuroim2::plot_overlay()`. `NULL` (default) lets `neuroim2` auto-tune
it; larger values push more of the low-value range toward transparency.}

\item{style}{Requested plot style. `"report"` is used when supported by the
installed `neuroim2`; otherwise it falls back to `"light"`.}

\item{on_mismatch}{Passed to [prepare_overlay()].}

\item{empty}{Action when no suprathreshold voxels are present.}

\item{draw}{Passed to `neuroim2::plot_overlay()`.}
}
\value{
A `stat_montage_result` list containing the plot object/list and
  render metadata.
}
\description{
Builds a thresholded or continuous volume montage using
`neuroim2::plot_overlay()` after
running [prepare_overlay()]. The function hard-errors by default when no
finite suprathreshold voxels are present, avoiding silently blank figures.
}

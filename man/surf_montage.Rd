% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/montage_surface.R
\name{surf_montage}
\alias{surf_montage}
\title{Render a Statistic Surface Montage}
\usage{
surf_montage(
  stat,
  surfatlas,
  output_file,
  threshold,
  tail = c("two_sided", "positive", "negative"),
  signed = TRUE,
  cap = NULL,
  views = c("lateral", "medial"),
  hemis = c("left", "right"),
  surface = "inflated",
  surface_space = "fsLR-32k",
  density_override = NULL,
  resolution_override = NULL,
  fun = c("avg", "nn", "mode"),
  sampling = c("midpoint", "normal_line", "thickness"),
  overlay_alpha = 0.45,
  palette = "cork",
  overlay_palette = if (isTRUE(signed)) "vik" else "inferno",
  width = 1400,
  height = 900,
  res = 144,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  plot_fun = NULL,
  projection = NULL,
  empty = c("error", "warning")
)
}
\arguments{
\item{stat}{Statistic `NeuroVol` or file path.}

\item{surfatlas}{Surface atlas with `lh_atlas`/`rh_atlas` geometry.}

\item{output_file}{PNG output path.}

\item{threshold}{Positive numeric overlay threshold.}

\item{tail}{Tail mode.}

\item{signed}{Logical; use symmetric overlay limits?}

\item{cap}{Optional shared cap for surface overlay colors.}

\item{views}{Surface views passed to `neuroatlas::plot_brain()`.}

\item{hemis}{Hemispheres passed to `neuroatlas::plot_brain()`.}

\item{surface}{Surface geometry name for `plot_brain()`.}

\item{surface_space, density_override, resolution_override}{Surface asset
overrides passed through the shared projection layer.}

\item{fun, sampling}{Projection controls passed to `neurosurf::vol_to_surf()`.}

\item{overlay_alpha}{Surface overlay alpha.}

\item{palette}{Base parcel palette.}

\item{overlay_palette}{Overlay palette.}

\item{width, height, res}{PNG device settings.}

\item{title, subtitle, caption}{Plot annotations.}

\item{plot_fun}{Advanced/testing hook. Defaults to `neuroatlas::plot_brain`.}

\item{projection}{Advanced/testing hook for a precomputed projection payload.}

\item{empty}{Action when no suprathreshold voxels are present.}
}
\value{
A `surf_montage_result` list containing the PNG path, projection,
  diagnostics, and render metadata.
}
\description{
Projects a thresholded statistic volume onto a surface atlas using the
existing `ce_overlay.R` projection layer and writes a surface montage PNG.
The projection layer reuses `.overlay_geom_cache`; this function does not add
a second asset cache.
}

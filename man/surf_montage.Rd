% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/montage_surface.R
\name{surf_montage}
\alias{surf_montage}
\title{Render a Statistic Surface Montage}
\usage{
surf_montage(
  stat = NULL,
  surfatlas,
  output_file,
  threshold,
  tail = c("two_sided", "positive", "negative"),
  signed = TRUE,
  cap = NULL,
  limits = NULL,
  support_mask = NULL,
  views = c("lateral", "medial"),
  hemis = c("left", "right"),
  surface = "inflated",
  surface_space = "fsLR-32k",
  density_override = NULL,
  resolution_override = NULL,
  fun = c("avg", "nn", "mode"),
  sampling = c("thickness", "midpoint", "normal_line"),
  interpolation = c("linear", "nearest", "legacy"),
  aggregate = c("mean", "closest", "mode"),
  depth = NULL,
  surface_smooth_fwhm = 0,
  cortex_mask = NULL,
  cortex_mask_source = NULL,
  anatomy_metric = NULL,
  anatomy_metric_source = NULL,
  medial_wall = c("shade", "mask", "outline"),
  camera = c("canonical", "presentation"),
  orientation_labels = TRUE,
  overlay_alpha = 0.85,
  palette = "cork",
  overlay_palette = if (isTRUE(signed)) "vik" else "lajolla",
  width = 3000,
  height = 1875,
  res = 300,
  render_device = c("auto", "ragg", "png", "pdf"),
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  plot_fun = NULL,
  projection = NULL,
  empty = c("error", "warning"),
  vals = NULL,
  legend_title = NULL,
  units = NULL
)
}
\arguments{
\item{stat}{Statistic `NeuroVol` or file path. Leave `NULL` when using
parcel-valued `vals`.}

\item{surfatlas}{Surface atlas with `lh_atlas`/`rh_atlas` geometry.}

\item{output_file}{PNG or PDF output path. PDF embeds the rasterized cortical
panels while retaining vector text, orientation marks, and legend.}

\item{threshold}{Positive numeric overlay threshold, or `NULL`/`NA` for a
continuous map.}

\item{tail}{Tail mode.}

\item{signed}{Logical; use symmetric overlay limits?}

\item{cap}{Optional shared cap for surface overlay colors.}

\item{limits}{Optional finite increasing display limits.}

\item{support_mask}{Optional logical/numeric mask in source-value order.}

\item{views}{Surface views passed to `neuroatlas::plot_brain()`.}

\item{hemis}{Hemispheres passed to `neuroatlas::plot_brain()`.}

\item{surface}{Surface geometry name for `plot_brain()`.}

\item{surface_space}{Surface-space metadata used when `surfatlas` does not
define `surface_space`. The default rendering path uses the atlas geometry
resolved by [neuroatlas::plot_brain()].}

\item{density_override, resolution_override}{Deprecated manual-projection
controls. They are ignored by the default `plot_brain()` path.}

\item{fun, sampling}{Projection controls forwarded to
[neuroatlas::plot_brain()] as `overlay_fun` and `overlay_sampling`.
Continuous publication output defaults to five samples through cortical
thickness rather than the historical Gaussian-KNN midpoint proxy.}

\item{interpolation}{Voxel interpolation for continuous statistics:
`"linear"` (trilinear publication default), `"nearest"`, or the historical
`"legacy"` KNN contract.}

\item{aggregate}{Aggregation across cortical-depth samples. The publication
default is `"mean"`; categorical `"mode"` remains distinct and is invalid
with linear interpolation.}

\item{depth}{Explicit white-to-pial fractions. Defaults to five fractions
from 0.1 through 0.9.}

\item{surface_smooth_fwhm}{Optional tangential smoothing in millimetres.
Zero disables it and is the publication default.}

\item{cortex_mask}{Optional explicit lh/rh cortex-domain mask forwarded to
the publication renderer. If NULL, neuroatlas resolves atlas provenance
without treating parcel label zero as a universal medial wall.}

\item{cortex_mask_source}{Provenance label for an explicit mask.}

\item{anatomy_metric}{Optional lh/rh sulcal-depth or curvature metric.}

\item{anatomy_metric_source}{Provenance label for an explicit anatomy metric.}

\item{medial_wall}{Medial-wall display policy.}

\item{camera}{Strict canonical or slightly oblique presentation camera.}

\item{orientation_labels}{Draw anterior/posterior marks.}

\item{overlay_alpha}{Surface overlay alpha. The publication default leaves
a small amount of anatomical context visible beneath the statistic.}

\item{palette}{Base parcel palette.}

\item{overlay_palette}{Overlay palette.}

\item{width, height, res}{PNG device settings. Defaults target a 10 by 6.25
inch, 300-dpi publication raster.}

\item{render_device}{Output backend: \code{"auto"} selects Cairo PDF for a
`.pdf` path, otherwise prefers \pkg{ragg} when it
is installed and otherwise uses the base PNG device; \code{"ragg"}
requires \pkg{ragg}; \code{"png"} forces the base device; and
\code{"pdf"} forces Cairo PDF.}

\item{title, subtitle, caption}{Plot annotations.}

\item{plot_fun}{Advanced/testing hook. Defaults to `neuroatlas::plot_brain`.}

\item{projection}{Deprecated advanced/testing hook for a precomputed
projection payload.}

\item{empty}{Action when no suprathreshold voxels are present.}

\item{vals}{Optional numeric vector of per-parcel statistic values. Named
vectors are matched to `surfatlas$ids`; unnamed vectors must be in
`surfatlas$ids` order.}

\item{legend_title}{Quantity name for static and interactive legends,
separate from the panel `title`. `NULL` defaults to `"Statistic"`.}

\item{units}{Optional units appended to the colorbar quantity label.}
}
\value{
A `surf_montage_result` list containing the PNG path, projection,
  diagnostics, and render metadata.
}
\description{
Renders a statistic volume as a surface overlay by passing the volume to
[neuroatlas::plot_brain()], which performs volume-to-surface projection
against the supplied surface atlas. Alternatively, pass parcel-valued
statistics in `vals` to render directly with `plot_brain(vals = )` without
a volume-to-surface projection. The older precomputed projection hook is
retained only for compatibility and testing, and is deprecated.
}

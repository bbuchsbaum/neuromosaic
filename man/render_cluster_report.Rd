% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cr_render.R
\name{render_cluster_report}
\alias{render_cluster_report}
\title{Render a Cluster Report}
\usage{
render_cluster_report(
  report_data,
  output_file = "cluster_report.pdf",
  template = NULL,
  quiet = TRUE
)
}
\arguments{
\item{report_data}{A list assembled by [cluster_report()] containing:
`cluster_table`, `cluster_parcels`, `mni_table`, `plots`, `params`.}

\item{output_file}{Path for the rendered output. Extension determines format
(`.pdf`, `.html`, or `.qmd`). `.qmd` output writes a companion
`_report-data.rds` sidecar in the same directory.}

\item{template}{Path to a custom Rmd/Qmd template. If `NULL`, uses the
bundled template matching `output_file`.}

\item{quiet}{Logical; suppress render progress messages? Default `TRUE`.}
}
\value{
The path to the rendered report (invisibly).
}
\description{
Renders a parameterized Rmd template to PDF or HTML, embedding all cluster
tables and time-course plots.
}
\details{
Quarto source output depends on a sidecar `_report-data.rds` file.
  Keep that file in the same directory as the generated `.qmd`, and render
  from that directory unless you edit the embedded `readRDS()` path.
}

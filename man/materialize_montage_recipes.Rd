% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/montage_recipe.R
\name{materialize_montage_recipes}
\alias{materialize_montage_recipes}
\title{Materialize Derived Montage Map Recipes}
\usage{
materialize_montage_recipes(
  manifest,
  cache_dir = NULL,
  overwrite = FALSE,
  validate = TRUE,
  check_files = TRUE
)
}
\arguments{
\item{manifest}{A render manifest data frame.}

\item{cache_dir}{Directory for materialized recipe maps. Defaults to a
session temp directory.}

\item{overwrite}{Logical; recompute recipe rows even when the cached file
already exists?}

\item{validate}{Logical; run [validate_manifest()] after materialization?}

\item{check_files}{Logical; passed to [validate_manifest()].}
}
\value{
A render manifest with recipe rows converted to file-backed `path`
  rows and a `map_hash` column populated when possible.
}
\description{
Converts recipe-backed render-manifest rows into path-backed rows by
evaluating each recipe and writing the resulting `NeuroVol` to disk. Existing
cache files are reused by default, making derived maps testable and stable
across report renders.
}
\details{
Recipe entries may be functions, structured lists, paths, or `NeuroVol`
objects. Function recipes are called with `row = <one-row manifest>` when
their formals include `row`, otherwise with no arguments. Structured recipes
are lists with a `fun` (or `function`) member and optional `args` and `key`
members.
}

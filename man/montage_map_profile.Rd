% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/montage_profile.R
\name{montage_map_profile}
\alias{montage_map_profile}
\title{Define Display Semantics for a Montage Map Quantity}
\usage{
montage_map_profile(
  id,
  label = id,
  display_mode = NULL,
  scale = NULL,
  center = NULL,
  domain = NULL,
  limits = NULL,
  palette_family = NULL,
  alpha_mode = NULL,
  support = NULL,
  units = NULL
)
}
\arguments{
\item{id}{Stable profile identifier. Using a namespaced identifier such as
`"mylab:reliability"` is recommended for project-specific quantities.}

\item{label}{Human-facing quantity label used by static and interactive
legends. This is distinct from the manifest's analysis/panel `label`.
A manifest `legend_title` overrides this label for an individual map.}

\item{display_mode}{Either `"thresholded"` or `"continuous"`.}

\item{scale}{Either `"diverging"` or `"sequential"`.}

\item{center}{Numeric center for a diverging scale. The current volume and
surface contract supports zero-centered diverging scales.}

\item{domain}{Optional legal value domain. Infinite endpoints are allowed.}

\item{limits}{Optional fixed finite display limits. When omitted, limits are
derived robustly from map values and clipped to `domain`.}

\item{palette_family}{Optional palette name or family understood by the
renderer.}

\item{alpha_mode}{Overlay opacity mode.}

\item{support}{Whether the map is shown over the full `"analysis"` support
or only the `"primary"` map's suprathreshold support.}

\item{units}{Optional colorbar units.}
}
\value{
A `montage_map_profile` object.
}
\description{
A map profile describes how a scientific quantity should be displayed. It
is an ordinary value object: pass profiles directly to
[resolve_montage_profiles()] or [render_montage_report()] rather than
registering global state.
}

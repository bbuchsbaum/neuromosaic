% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/montage_explorer.R
\name{montage_explorer_data}
\alias{montage_explorer_data}
\title{Build Explorer Data for a Montage Report}
\usage{
montage_explorer_data(
  report_data = NULL,
  manifest = NULL,
  panels = NULL,
  qc = NULL,
  design = NULL,
  signals = NULL
)
}
\arguments{
\item{report_data}{A report data list produced internally by
[render_montage_report()] or [montage_explorer()].}

\item{manifest}{Optional manifest override when `report_data` is not
supplied.}

\item{panels}{Optional named panel list keyed by `map_id`.}

\item{qc}{Optional QC data frame keyed by `map_id`.}

\item{design}{Optional design table exposed to signal-plot code.}

\item{signals}{Optional long data frame of design-linked signal values. When
it contains a `map_id` column, the Shiny explorer filters it to the selected
map row.}
}
\value{
A list with `manifest`, `panel_index`, `panels`, `cluster_tables`,
  `qc`, `design`, and `signals`.
}
\description{
Converts montage report data into the row-indexed structure used by
[montage_explorer()]. The same render manifest remains the source of truth:
each row maps to panel images, a peak/cluster table, QC metadata, and optional
signal data.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/montage_fmrigds.R
\name{fmrigds_render_manifest}
\alias{fmrigds_render_manifest}
\title{Build a Montage Render Manifest from an fmrigds GDS}
\usage{
fmrigds_render_manifest(
  gds,
  assay = "beta",
  materialize_dir = NULL,
  map_id_cols = NULL,
  label_cols = map_id_cols,
  stat_kind = .fmrigds_default_stat_kind(assay),
  units = assay,
  signed = TRUE,
  threshold = NULL,
  p = NULL,
  tail = "two_sided",
  connectivity = "18-connect",
  min_cluster_size = 10L,
  level = "group",
  validate = TRUE,
  check_files = TRUE
)
}
\arguments{
\item{gds}{A realised `fmrigds` GDS object.}

\item{assay}{Assay name to materialize.}

\item{materialize_dir}{Directory where assay maps are written. Defaults to a
temporary directory.}

\item{map_id_cols, label_cols}{GDS metadata columns used to build `map_id` and
`label`. Defaults prefer `contrast/model/variant`, falling back to
`contrast/subject`.}

\item{stat_kind, units, signed, threshold, p, tail, connectivity, min_cluster_size}{Default render-manifest policy columns.}

\item{level}{Manifest `level` value. Defaults to `"group"`.}

\item{validate}{Logical; run [validate_manifest()] before returning?}

\item{check_files}{Logical; require materialized paths to exist?}
}
\value{
A render manifest data frame.
}
\description{
Converts a realised `fmrigds` GDS assay into the storage-agnostic render
manifest consumed by [render_montage_report()]. The selected assay is
materialized to NIfTI files, while GDS map metadata such as `contrast`,
`model`, and `variant` is carried into the manifest.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/montage_policy.R
\name{resolve_montage_policy}
\alias{resolve_montage_policy}
\title{Resolve Montage Policy Defaults Against a Manifest}
\usage{
resolve_montage_policy(
  manifest,
  policy = montage_policy(),
  empty = c("error", "warning"),
  stat_maps = NULL,
  load_maps = FALSE
)
}
\arguments{
\item{manifest}{A render manifest data frame.}

\item{policy}{A `montage_policy` object.}

\item{empty}{Action when overlay QC finds a map with no suprathreshold
voxels: `"error"` (default) or `"warning"`. Forwarded to
[validate_manifest()]; relevant when `load_maps = TRUE` or in-memory
map/parcel sources trigger overlay checks.}

\item{stat_maps}{Optional list of statistic maps or numeric statistic
vectors used to resolve FDR `q` thresholds. Required when `policy$q` or a
manifest `q` column applies to any row without an explicit `threshold`.}

\item{load_maps}{Logical; allow [validate_manifest()] to read path-backed
maps for overlay QC, including manifests mixing paths and in-memory sources.}
}
\value{
The manifest with `effective_threshold`, `effective_tail`,
  `effective_connectivity`, `effective_min_cluster_size`, and `cap_key`
  columns added.
}
\description{
Applies a [montage_policy()] to a render manifest, preserving per-row manifest
overrides where present.
}
